import { GrowAnimationDirective } from './grow-animation.directive';

describe('GrowAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new GrowAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
